import { SelectedDirective } from './selected.directive';

describe('SelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedDirective();
    expect(directive).toBeTruthy();
  });
});
